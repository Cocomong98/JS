/*
연산자

== 는 값만 비교하고
=== 는 값과 자료형도 본다

!= 는 값만 다른걸 보고
!-- 는 값과 자료형도 본다

x < y 는 x가 y보다 사전순서상 앞에 온다는 것
x <= y 는 x가 y보다 사전순서상 앞에 오거나 함께 있다

x > y 는 y가 사전순서상 앞에 온다
x >= y 는 y가 사전순서상 앞에 오거나 함께 있다
*/

/*

*/
